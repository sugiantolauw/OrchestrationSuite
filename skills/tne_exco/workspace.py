"""render_workspace(run_id) wires this Skill's run into /workspace/tne -- that
wiring is P4's job (CLAUDE.md §8). Left as an explicit stub rather than a fake
render so a caller fails loudly instead of getting a silently empty page."""

from __future__ import annotations


def render_workspace(run_id: str):
    raise NotImplementedError("P4")
