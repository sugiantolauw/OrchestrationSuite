# Changelog (manifest.yaml carries no structured field for this --
# additionalProperties: false in orchestrator/schemas/manifest.schema.json
# -- so it is recorded here as a comment; content.lock in this directory
# pins the version this content_hash belongs to):
#   2.2.0-draft (2026-09-25): contract.yaml's parameters: block (population_of_
#     interest as three grouped references -- independent review 2026-09-25
#     item 1 "run inputs", docs/specs/P7_mapping_authoring_design.md §1.3).
#   2.1.0-draft (2026-09-25): findings.yaml's optional metric_descriptions
#     block (BUG-SKILLVER-1 fix -- commit 7d46554 changed findings.yaml by
#     154 lines without bumping this file, which broke every fresh
#     SKILL-001 run against the shared ledger with SkillVersionConflict).
id: SKILL-001
name: ExCo T&E Executive Diligence
domain: Travel & Expenses
version: "2.2.0-draft"
owner: Internal Audit
status: draft
description: >
  Assess executive travel and entertainment spend against policy. 14 deterministic
  tests covering pre-approval, preferred suppliers, split claims, duplicates,
  missing receipts, attendee validation, approver review quality, and daily
  spend limits.
