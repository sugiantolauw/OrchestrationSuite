# Runtime
# Pinned exactly (not >=): the connected App's UI was built and E2E-tested
# against dash==2.17.1 / dash-bootstrap-components==1.6.0. An unpinned
# `>=` install resolves to a much newer major (dash 4.x, dbc 2.x as of this
# writing) whose DOM/behaviour differs from what was validated -- pin here
# and in whatever requirements file scripts/deploy_app.py ships, so a
# deploy never silently picks up a different major version.
dash==2.17.1
dash-bootstrap-components==1.6.0
plotly>=5.18
pandas>=2.1
numpy>=1.26
pyyaml>=6.0
python-dotenv>=1.0
jsonschema>=4.0

# LLM client (Databricks Model Serving OpenAI-compatible surface)
openai>=1.40.0

# Databricks
databricks-sdk>=0.30.0
# Pinned exactly, not >=: the P2/P3 gate review found live monetary values
# corrupted to float32 precision in Delta. persistence_delta.py now binds
# every monetary/metric parameter as an explicit DoubleParameter (never
# relying on the driver's implicit float->DOUBLE inference), and that
# inference behaviour -- and the DoubleParameter/dbsql_parameter_from_primitive
# API itself -- is exactly what CLAUDE.md warns pinning protects: an
# unpinned `>=3.0` install can silently resolve to a version whose default
# float handling or parameter API differs from what was verified live
# (tests/test_delta_sql.py::test_float_precision_round_trips_live).
databricks-sql-connector==4.5.0

# Observability + evaluation
mlflow>=2.16.0

# Exports
python-pptx>=1.0.2
xlsxwriter>=3.2.9
openpyxl>=3.1
pillow>=12.2.0
lxml>=6.1.1
# Per-run row snapshots for /workspace/tne (orchestrator/frames.py, P4 perf
# fix) -- was already resolved transitively via databricks-sql-connector, now
# depended on directly for DataFrame.to_parquet/read_parquet.
pyarrow>=17.0

# Dev / test
pytest>=8.0
pytest-cov>=5.0
playwright>=1.45
