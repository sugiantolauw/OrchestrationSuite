{
  "type": "object",
  "properties": {
    "timezone": {"type": "string"},
    "sources": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "format": {"type": "string", "enum": ["xlsx", "csv", "parquet", "uc_table", "upload"]},
          "sheet": {"type": ["string", "integer"]},
          "file": {"type": "string"},
          "header_trim": {"type": "boolean"},
          "entry_key": {"type": "array", "items": {"type": "string"}, "minItems": 1},
          "entry_key_repeats": {"type": "boolean"},
          "optional": {"type": "boolean"},
          "columns": {
            "type": "object",
            "additionalProperties": {
              "type": "object",
              "properties": {
                "type": {
                  "type": "string",
                  "enum": ["string", "integer", "number", "date", "datetime", "boolean"]
                },
                "nullable": {"type": "boolean"},
                "pii": {"type": "boolean"},
                "trim_values": {"type": "boolean"},
                "allowed_values": {"type": "array"},
                "required_values": {"type": "array"}
              },
              "required": ["type"],
              "additionalProperties": false
            }
          }
        },
        "required": ["format", "columns"],
        "additionalProperties": false,
        "if": {
          "properties": {"format": {"enum": ["xlsx", "csv", "parquet"]}},
          "required": ["format"]
        },
        "then": {"required": ["file"]}
      }
    },
    "parameters": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "references": {"type": "array", "items": {"type": "string"}, "minItems": 1},
          "kind": {"type": "string", "enum": ["id_list", "table", "mapping"]},
          "item_type": {"type": "string", "enum": ["integer", "string"]},
          "columns": {"type": "object", "additionalProperties": {"type": "string"}},
          "group": {"type": "string"}
        },
        "required": ["references", "kind", "group"],
        "additionalProperties": false
      }
    }
  },
  "required": ["timezone", "sources"],
  "additionalProperties": false
}
