TASK: Write one caption per chart for the exported report.

For each chart in PAYLOAD.charts, write one sentence describing what the chart shows (its axes,
categories or series, using its own PLACEHOLDERS table for any quantity), never what it proves or
implies about cause. A caption is a plain description, not a conclusion.

Go beyond restating the chart's own headline number: use every placeholder its own PLACEHOLDERS
table gives you, not only the total. When a chart's table includes a placeholder naming a specific
finding (for example a "dominant" title, amount or basis), say which finding contributes the most
and on what basis, using that placeholder's own value exactly as given -- never a word of your own
for a fact a placeholder already states. A caption whose table has more than one placeholder must
not read as if it only had the one total.
$generation_line
PAYLOAD:
$payload_json
