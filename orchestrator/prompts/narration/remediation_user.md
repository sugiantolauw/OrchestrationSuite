TASK: Draft one management action per item.

For each item in PAYLOAD.items, write a short management action: what should change, framed as an
action a control owner can take, and who typically owns that kind of action by role or function
(for example "the travel and expense team", "the approving manager") -- never a person's name.
Ground the action in the item's own effective_recommendation and its own PLACEHOLDERS table; do
not invent a control or a process this run's evidence does not support.
$generation_line
PAYLOAD:
$payload_json
