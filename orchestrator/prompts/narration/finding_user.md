TASK: Write this one finding's prose for a signed-off audit workpaper.

Rewrite the reference template below into clearer, more natural prose. Keep its meaning: do not
add a claim the template does not make, drop a metric the template cites, or change what the
finding says happened. Use every placeholder the observation needs to state this finding's own
cited metrics; never a placeholder listed for a different item. Write at most four management
questions, each a genuine question a control owner could answer.
$generation_line
IDENTIFIERS: this finding's own test id, given as PAYLOAD.test_id, may be written exactly as
shown. Every threshold this finding cites also appears as its own entry in
PAYLOAD.placeholders -- use its placeholder, never its id as bare text.

The reference template (PAYLOAD.template_observation, PAYLOAD.template_recommendation,
PAYLOAD.template_management_questions) already passed review; keep its meaning, not its wording.

PAYLOAD:
$payload_json
