TASK: Write the executive summary for this audit's workpaper.

Write two or three short paragraphs an executive will read verbatim, framed by the themes named in
PAYLOAD.theme_titles and the top findings in PAYLOAD.top_findings. The first paragraph must use
{count:run_finding_count} and, only when PAYLOAD.placeholders gives it a value, the headline
placeholder {money:run_exposure_headline}. Do not list every finding; describe the shape of what
this run found and why it matters. Do not recommend a specific remediation here -- that belongs to
each finding's own recommendation, not the summary.
$generation_line
PAYLOAD:
$payload_json
