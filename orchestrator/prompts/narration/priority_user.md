TASK: Write one sentence per item explaining why it matters to the audit committee.

PAYLOAD.items lists this run's findings and candidates in Python's own priority order -- do not
reorder them, restate that order, or describe an item's position ("first", "top", "the highest").
For each item, write one sentence explaining why it matters, grounded in its own severity and its
own PLACEHOLDERS table (particularly its exposure amount, where it has one).
$generation_line
PAYLOAD:
$payload_json
