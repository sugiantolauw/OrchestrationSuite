"""Eval Gate — deprecated stub.

The findings pipeline now uses narrative.generate_findings() + guardrail.verify_all_findings()
directly from app.py. This file is kept as a no-op placeholder so existing test imports
don't crash the app on startup.
"""
